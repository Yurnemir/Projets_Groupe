
monApp.controller("listeClientsCtrl", function($scope) {
	$scope.message = "listeClientsCtrl";
});

monApp.controller("rechercheClientCtrl", function($scope) {
	$scope.message = "rechercheClientCtrl";
});

monApp.controller("ajoutClientCtrl", function($scope) {
	$scope.message = "ajoutClientCtrl";
});
